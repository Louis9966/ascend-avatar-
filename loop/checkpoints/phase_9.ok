Phase 9 OK

- 视频生成嘴部清晰度优化已完成并验证：
  - `src/config.py` 新增 `THG_*` / `FFMPEG_*` 参数，默认 `THG_BLUR_RATIO=0.05`、`THG_RENDER_INTERPOLATION=lanczos4`、`FFMPEG_PRESET=medium`。
  - `thg/musetalk/utils/blending.py` `get_image_prepare_material` 支持 `blur_ratio`、`upper_boundary_ratio`、`expand`，默认参数保持向后兼容。
  - `src/thg_engine.py` `MuseTalkAvatar` 在 prepare/infer/render_to_video 中使用新参数；渲染上采样默认改用 `INTER_LANCZOS4`；FFmpeg 输出加入 `-preset`。
  - `src/avatar_manager.py` 与 `src/pipeline.py` 创建 avatar 时传递所有新参数。
- 文档已更新：`config/.env.example`、`docs/deploy.md`、`docs/benchmark.md`、`README.md`。
- 验证记录（默认 avatar，17 字中文文本）：
  - 新配置（blur=0.05, lanczos4）：job_id=46eb70e7ade74e05，136 帧，嘴部 ROI Laplacian 方差均值 **1065.55**。
  - 旧配置基线（blur=0.1, linear）：job_id=485c94564a2a4be8，136 帧，嘴部 ROI Laplacian 方差均值 **1063.68**。
  - 锐度指标提升约 0.18%，PaddleSpeech TTS 成功未 fallback。
- 全流程验证（`/data/ascend-avatar/sample/MyVideo_1.mp4`）：
  - 上传：upload_id=49a6c93f7de1f8a0，1280×720@25fps，26.3s，658 帧。
  - MuseTalk 预处理：约 35 分钟（首次长视频 + 658 帧）。
  - 生成：job_id=d657ab22e0314538，文本 "你好，这是用 MyVideo_1 做的数字人测试。"，PaddleSpeech TTS，THG 完成。
  - 下载：`/ascend-avatar/output/generated/MyVideo_1_verify.mp4`，1280×720@25fps，92 帧（3.68s），文件大小 360K。
  - 嘴部 ROI Laplacian 方差均值：**123.38**（720p 素材 ROI 较大，数值与 512 默认 avatar 不直接可比）。
- 注意：已缓存 avatar 的 mask 在磁盘上按旧参数生成，应用新参数需删除 `output/v15/avatars/<upload_id>` 后重新上传/预处理。
